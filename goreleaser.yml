builds:
  - binary: terraform-provider-openfaas_v{{.Version}}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
archives:
  - id: terraform-provider-openfaas_v{{.Version}}-{{.Arch}}
    format: zip
    files:
      - none*
